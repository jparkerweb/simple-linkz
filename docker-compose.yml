version: '3.8'
services:
  simple-linkz:
    image: jparkerweb/simple-linkz:latest
    # Or build locally:
    # build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - PORT=3000
      - SESSION_SECRET=${SESSION_SECRET:-}
    restart: unless-stopped
